package org.example.cyberwatch.model.entitys.forms;

// This class can be used to log significant actions and changes within the system for auditing purposes.
public class AuditEvents {
    // This class is intended to log significant actions and changes within the system for auditing purposes.
    // It can include fields such as:
    // - id (primary key)
    // - timestamp (when the event occurred)
    // - user (who performed the action)
    // - actionType (what type of action was performed, e.g., "CREATE_REPORT", "APPROVE_FORM")
    // - details (additional information about the event, such as which report was created or which form was approved)
    // - messages?
}
