-- Ersätter personlig data från V6 med fake testdata (GDPR/PII-åtgärd).
-- V6 kan inte ändras eftersom den redan körts mot databasen.
-- NOTERA: Vi behöver lägga till våran egna Gmail-adress manuellt
-- via SQL för att kunna logga in via Google OAuth2 som Eric sa.

DELETE FROM staff WHERE social_security_number = '19900101-1234';

INSERT INTO staff (social_security_number, first_name, last_name, email, phone_number, role, department, password)
VALUES ('19900101-0000', 'Test', 'Admin', 'admin@example.com', '0000000000', 'ADMIN', 'MANAGEMENT', '$2a$10$w5pG/fM/yT.x/jZ0j2/H/.8yK5PzX3Y/O5F7y/6C6e9zX6y/O5F7y')
ON CONFLICT (social_security_number) DO NOTHING;